import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Return Policy',
  description: 'Intetkøns Return Policy',
}

const Layout = ({ children }: { children: React.ReactNode }) => {
  return children
}

export default Layout
